<h1>My Lists</h1>

<%= link_to "Create a new list", new_list_path, class: "btn btn-primary mb-3" %>

<div class="lists">
  <% @lists.each do |list| %>
    <div class="list">
      <h3><%= link_to list.name, list_path(list)  %></h3>
    </div>
  <% end %>
</div>
